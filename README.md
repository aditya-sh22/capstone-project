# Python + Flask + MySQL Two-Tier Application

## 🚀 Overview
This project is a simple two-tier web application:
- **Frontend:** HTML employee form
- **Backend:** Python Flask
- **Database:** MySQL
- **Dockerized** with Dockerfile and docker-compose

## 📦 Project Structure
```
app.py
Dockerfile
Dockerfile.ci
docker-compose.yml
requirements.txt
schema.sql
templates/
 └── form.html
```

## 🛠️ Setup Without Docker
1. Install Python dependencies:
```
pip install -r requirements.txt
```

2. Import the SQL schema into your MySQL:
```
mysql -u root -p employee_db < schema.sql
```

3. Run Flask app:
```
python app.py
```

Access at: http://localhost:5000

## 🐳 Setup With Docker
### 1. Build & Run using docker-compose:
```
docker-compose up --build
```

Flask App → http://localhost:5000  
MySQL → localhost:3306

## 🔐 Environment Variables (auto handled by docker-compose)
- DBHOST
- DBPORT
- DBUSER
- DBPWD
- DATABASE

## 🧪 Dockerfile for CI (Trivy scanning)
`Dockerfile.ci` is meant for GitHub Actions scanning only.

## 📄 SQL Schema
Located in `schema.sql`

## 👍 Author
Generated by ChatGPT
